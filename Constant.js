const CAR = {
    CARNAME_MIN: 1,
    CARNAME_MAX: 5,
}

const RACE = {
    GOSTANDARD: 4,
}

const MESSAGE = {
    ERROR_CARNAME_LENGTH: "[ERROR]5자 이내로 입력해주세요.",
    ERROR_RACECOUNT_LENGTH: "[ERROR]0회 이상으로 입력해주세요.",
}

module.exports = { CAR, RACE, MESSAGE } ;
